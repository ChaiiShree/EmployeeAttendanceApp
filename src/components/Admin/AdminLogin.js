import React from 'react';
import Login from '../common/Login';

const AdminLogin = ({ navigation }) => {
    return <Login navigation={navigation} role="admin" />;
};

export default AdminLogin;
