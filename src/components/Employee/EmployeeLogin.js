import React from 'react';
import Login from '../common/Login';

const EmployeeLogin = ({ navigation }) => {
    return <Login navigation={navigation} role="employee" />;
};

export default EmployeeLogin;
