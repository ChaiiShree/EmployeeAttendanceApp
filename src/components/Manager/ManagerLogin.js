import React from 'react';
import Login from '../common/Login';

const ManagerLogin = ({ navigation }) => {
    return <Login navigation={navigation} role="manager" />;
};

export default ManagerLogin;
